<UserControl x:Class="PersonalToolbox.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="菜单首页" FontSize="26" FontWeight="SemiBold" Foreground="#1D2733" />
            <TextBlock Text="选择一个工具开始处理本地任务。" Margin="0,6,0,0" Foreground="#667085" />
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tools}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.OpenToolCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"
                            Width="280"
                            Height="132"
                            Margin="0,0,14,14"
                            Padding="16"
                            Background="White"
                            BorderBrush="#D9DEE7"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#1D2733" />
                            <TextBlock Text="{Binding Description}" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#667085" />
                            <TextBlock Text="打开" Margin="0,18,0,0" Foreground="#2563EB" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
